---

- name: Install Kubernetes on ubuntu
  hosts: cluster
  become: yes
  vars:
    kubernetes_version: "1.34"
    cni_plugin: "calico"
  roles:
    - prereqs
    - containerd
    - kubernetes
    - cni

